[gd_scene load_steps=5 format=3]

[ext_resource type="Script" path="res://scenes/core/Main.gd" id="1"]
[ext_resource type="PackedScene" path="res://scenes/core/HUD.tscn" id="2"]

[sub_resource type="Shader" id="1"]
code = """
shader_type canvas_item;

void fragment() {
    vec2 uv = UV * 2.0 - vec2(1.0);
    float dist = length(uv);
    float alpha = smoothstep(0.95, 1.0, dist);
    COLOR = vec4(0.0, 0.0, 0.0, alpha);
}
"""

[sub_resource type="ShaderMaterial" id="2"]
shader = SubResource("1")

[node name="Main" type="Node"]
script = ExtResource("1")

[node name="SubViewportContainer" type="SubViewportContainer" parent="."]
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
stretch = true
mouse_filter = 1

[node name="SubViewport" type="SubViewport" parent="SubViewportContainer"]
handle_input_locally = false
size = Vector2i(1280, 720)
usage = 0
render_target_update_mode = 2

[node name="Mask" type="ColorRect" parent="SubViewportContainer"]
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
mouse_filter = 2
material = SubResource("2")

[node name="CanvasLayer" parent="." instance=ExtResource("2")] 

[editable path="CanvasLayer"]
